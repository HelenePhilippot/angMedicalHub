import { PraticienSpecialiteKey } from './praticien-specialite-key';

describe('PraticienSpecialiteKey', () => {
  it('should create an instance', () => {
    expect(new PraticienSpecialiteKey()).toBeTruthy();
  });
});
